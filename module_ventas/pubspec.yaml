publish_to: none # ← debe ir primero, sin sangría

name: module_ventas
description: Módulo de ventas
version: 0.1.0
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../shared
